# 인노 (Inno) — 본 앱 기획서

> 가공식품의 원재료 함량을 비교해 "진짜 뭐가 들어있는지" 보여주는 모바일 앱.
> 다이어트가 아니라 **함량 진실** 중심.

## 0. 브랜드 정보

- **서비스명**: 인노 (Inno)
- **풀 브랜드명**: 인노 — 라벨 그대로
- **태그라인**: 함량 그대로, 인노
- **표기 규칙**:
  - 한글: `인노`
  - 영문: `Inno` (대문자 시작, 본문에서는 소문자 `inno` 허용)
  - 도메인/핸들: `inno` 우선, 안 되면 `innofood` / `getinno` / `useinno`
  - 코드 식별자: `inno` (소문자 통일)
- **앱스토어 등록명**:
  - iOS: `인노 - 라벨 그대로 (함량, 성분 비교)`
  - Android: `인노 - 식품 라벨, 함량, 성분 비교`
- **카피 가이드라인**:
  - 사용 어휘: 함량, 비율, 원재료, 라벨, 진짜, 그대로, 비교
  - 금지 어휘: 다이어트, 건강한, 저칼로리, 슈퍼푸드, 리얼푸드 (마이리얼푸드 영역 회피)
  - 톤: 트렌디하고 샤프, 단정적이지 않고 사실 제시

---

## 1. 제품 정의

### 1줄 정의
"같은 카테고리의 가공식품을, 표시된 원재료 함량 기준으로 비교·정렬해주는 앱"

### 핵심 차별점
- 칼로리/탄단지 위주의 기존 다이어트 앱과 다름
- **"진짜 고기 함량", "진짜 과즙 함량"** 같은 라벨 진실을 비교 축으로
- 카테고리마다 핵심 지표가 다름 (햄=돈육%, 주스=과즙%, 초콜릿=카카오%)

### 메인 타겟
- 1차: 가공식품 구매 시 함량을 따지고 싶은 일반 소비자
- 채널별 세부 타겟:
  - 주스/음료 → 30-40대 부모, 건강 의식 있는 20-30대
  - 햄/소시지 → 20-30대 자취생, 30-40대 가성비 추구
  - 만두 → 1인가구, 직장인

---

## 2. MVP 스코프 (1차 출시)

### 2.1 카테고리
**주스/음료 + 햄/소시지 + 만두** 3개로 시작

(카테고리 선정 근거는 별도 논의에서 합의된 사항)

### 2.2 제품 수
- 카테고리당 100-150개
- 총 300-450개 (메이저 제조사 중심)

### 2.3 필수 기능
1. 카테고리 진입 후 **원재료 함량 순 정렬**
2. 제품 상세 페이지 (원재료, 영양성분, 카테고리 평균 대비)
3. **2-3개 제품 비교** 화면
4. 검색 (제품명, 제조사)
5. 카테고리/제품 둘러보기

### 2.4 제외 기능 (출시 후 추가)
- 회원가입/로그인 (익명 시작)
- 리뷰/평점
- 광고/제휴 커머스 (D+90 이후)
- 바코드 스캔 (있으면 좋지만 출시 필수 X)
- 즐겨찾기/공유
- 푸시 알림
- 추정 원가율 (D+90 이후 차별화 기능으로)
- 알레르기/식단 개인화

---

## 3. 화면 설계

### 3.1 화면 목록 (MVP)

1. **홈** — 카테고리 그리드 + 오늘의 인사이트 카드
2. **카테고리 랭킹** — 제품 리스트, 정렬 가능
3. **제품 상세** — 함량/영양/원재료/비교 제품
4. **비교 화면** — 2-3개 제품 나란히
5. **검색** — 제품/제조사 검색
6. **콘텐츠 상세** — 인사이트 글 (예: "오렌지주스 함량 전수조사")
7. **카테고리 가이드** — 카테고리별 "함량 읽는 법" 정적 콘텐츠

### 3.2 홈 화면

```
┌──────────────────────────────────┐
│ [검색바: "제품, 성분 검색"]       │
├──────────────────────────────────┤
│ 빠른 액션                          │
│ ┌────────┐ ┌────────┐            │
│ │ 비교하기│ │ 바코드 │            │
│ └────────┘ └────────┘            │
├──────────────────────────────────┤
│ 카테고리                           │
│ ┌────┐ ┌────┐                    │
│ │주스│ │ 햄 │                    │
│ └────┘ └────┘                    │
│ ┌────┐ ┌────┐                    │
│ │만두│ │ +  │                    │
│ └────┘ └────┘                    │
├──────────────────────────────────┤
│ 오늘의 발견                        │
│ ┌──────────────────────────────┐ │
│ │ "허쉬 초콜릿엔 카카오버터가  │ │
│ │  들어있지 않다?"             │ │
│ │ [읽기 →]                     │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

### 3.3 카테고리 랭킹 (가장 중요한 화면)

```
┌──────────────────────────────────┐
│ ← 햄·소시지         127개   🔍  │
├──────────────────────────────────┤
│ [돈육 함량↓][나트륨↑][가격↓][첨가물↓]│
├──────────────────────────────────┤
│ ┌────┐ 스팸 클래식 200g          │
│ │IMG │ CJ제일제당        91%🟢  │
│ └────┘ 5,000원         돈육      │
├──────────────────────────────────┤
│ ┌────┐ 로스팜 200g               │
│ │IMG │ 롯데햄            89%🟢  │
│ └────┘ 4,500원         돈육      │
├──────────────────────────────────┤
│ ┌────┐ 리챔 오리지널 200g        │
│ │IMG │ 동원F&B           87%    │
│ └────┘ 4,800원         돈육      │
├──────────────────────────────────┤
│ ┌────┐ A사 런천미트 340g         │
│ │IMG │ A식품             52%🔴  │
│ └────┘ 6,500원         돈육      │
└──────────────────────────────────┘
```

핵심 UX:
- 첫 화면 진입 시 기본 정렬 = "함량 높은 순"
- 색상 신호: 85%↑ 초록 / 70-84% 검정 / 70%↓ 빨강
- 카드 탭 → 제품 상세

### 3.4 제품 상세

세로 스크롤 구조:

**섹션 1 - 제품 헤더**
- 이미지 캐러셀 (1-3장)
- 제품명 / 제조사 / 용량
- 평균가, "비교에 추가" 버튼

**섹션 2 - 핵심 지표 하이라이트**
```
┌──────────────────────────┐
│ 돈육 함량                  │
│      87%                  │
│ 카테고리 평균보다 +5%     │
│ 127개 중 3위              │
└──────────────────────────┘
```

**섹션 3 - 원재료 시각화 (가로 막대)**
- 돈육 87% ████████████████░░
- 정제수 8% ██░
- 정제소금 2% ░
- 기타 3% ░

**섹션 4 - 영양성분표**
- 100g / 1회 제공량 토글
- 칼로리, 단백질, 지방, 나트륨, 당류

**섹션 5 - 주의 성분**
- 첨가물 리스트 (탭하면 성분 설명)
- 알레르기 유발 성분

**섹션 6 - 같은 카테고리 추천**
- "함량 더 높은 제품 3개"
- "비슷한 가격대 제품 3개"

### 3.5 비교 화면

상단 비교 슬롯 (2-3개) + 항목별 비교 테이블

```
┌──────────────────────────────────┐
│ [제품A] [제품B] [+추가]            │
├──────┬──────────┬─────────────────┤
│      │  스팸    │  리챔           │
├──────┼──────────┼─────────────────┤
│돈육  │ 91% 🟢   │ 87%             │
│나트륨│ 720mg    │ 680mg 🟢       │
│칼로리│ 280kcal  │ 310kcal         │
│가격  │ 1450/100g│ 1200/100g 🟢   │
│첨가물│ 5개      │ 4개 🟢          │
└──────┴──────────┴─────────────────┘
```

각 행에서 가장 좋은 값에 🟢 표시 (배경 강조).

### 3.6 검색

- 검색바에 입력하면 실시간 결과
- 결과: 제품명 / 제조사 / 카테고리별 그룹
- 빈 결과: "이 제품이 없나요? [등록 요청]"

---

## 4. 데이터 흐름

본 앱은 데이터 입력 도구와 **동일 DB를 읽기 전용으로 사용**.

```
[관리자 어드민] → [PostgreSQL] → [본 앱 API] → [모바일 앱]
                    ↑
              [크롤러: 가격 갱신]
```

### 4.1 API 엔드포인트 (최소)

```
GET  /api/categories                  # 카테고리 목록
GET  /api/categories/:slug/products   # 카테고리 제품 목록 (정렬/필터 쿼리)
GET  /api/products/:id                # 제품 상세
GET  /api/products/compare?ids=a,b,c  # 비교 (2-3개)
GET  /api/products/search?q=...       # 검색
GET  /api/content/insights            # 콘텐츠 카드 (홈 노출용)
GET  /api/content/insights/:slug      # 콘텐츠 상세
POST /api/products/requests           # 제품 등록 요청 (사용자)
```

### 4.2 정렬/필터 (카테고리 랭킹 화면)

```
GET /api/categories/ham/products
  ?sort=key_metrics.pork_content_pct:desc
  &filter[manufacturer]=CJ제일제당
  &filter[max_price]=5000
  &page=1&size=20
```

카테고리마다 정렬 가능한 필드가 다름. 카테고리 메타에 정의된 `sortable_fields`만 허용.

---

## 5. 기술 스택 권장

- **앱**: React Native (또는 Flutter) — 크로스 플랫폼
- **백엔드**: 어드민과 동일 (Next.js API or NestJS)
- **DB**: PostgreSQL (어드민과 공유)
- **이미지 CDN**: Cloudflare R2 또는 S3 + CloudFront
- **분석**: Mixpanel 또는 Amplitude (무료 티어)
- **푸시(추후)**: Firebase Cloud Messaging
- **배포**: TestFlight + Play Console Internal Test

---

## 6. 핵심 데이터 모델 (앱이 읽는 형태)

```typescript
type Product = {
  id: string;
  barcode?: string;
  name: string;
  manufacturer: { id: string; name: string };
  category: { slug: string; name: string };
  volume: { value: number; unit: 'g' | 'ml' | 'ea' };
  price: { msrp?: number; avgOnline?: number };
  images: { product: string[]; label: string[] };

  ingredients: {
    rawText: string;
    parsed: Array<{ name: string; pct?: number; origin?: string }>;
  };
  additives: string[];
  allergens: string[];

  nutrition: {
    baseUnit: '100g' | '100ml' | 'serving';
    baseAmount: number;
    kcal?: number;
    protein?: number;
    carb?: number;
    sugar?: number;
    fat?: number;
    saturatedFat?: number;
    sodium?: number;
  };

  keyMetrics: Record<string, number | boolean>;
  // 예: 햄 = {pork_content_pct: 87}
  // 예: 주스 = {juice_content_pct: 100, hfcs: false, artificial_flavor: false}

  categoryStats: {
    rank: number;          // 카테고리 내 순위 (핵심 지표 기준)
    total: number;
    avgKeyMetric: number;
  };
};

type Category = {
  slug: string;            // 'ham', 'juice', 'dumpling'
  name: string;            // '햄/소시지'
  keyMetric: {
    field: string;         // 'pork_content_pct'
    label: string;         // '돈육 함량'
    unit: '%' | 'g' | 'mg';
    higherIsBetter: boolean;
  };
  sortableFields: Array<{
    field: string;
    label: string;
    higherIsBetter: boolean;
  }>;
  insightThresholds: {     // 색상 신호용
    good: number;          // 85
    warning: number;       // 70
  };
};
```

---

## 7. 핵심 UX 원칙

1. **3초 안에 인사이트** — 카테고리 진입 즉시 "어 이 제품이 함량 높네"가 보여야 함
2. **숫자가 주인공** — 함량 %는 가장 큰 폰트로
3. **색상은 의미 있게만** — 초록/빨강은 함량 신호일 때만, 장식 X
4. **다이어트 어휘 금지** — "건강한", "다이어트", "저칼로리" 표현 의도적으로 회피. "함량", "비율", "원재료" 어휘 사용
5. **광고는 후순위** — 1차 출시엔 광고 없음, D+90 이후 도입 시 명확히 "광고" 라벨
6. **함량 평가는 중립적으로** — "이게 좋다/나쁘다" 단정 X, "함량 X%, 카테고리 평균은 Y%" 식 사실 제시

---

## 8. 콘텐츠 시스템 (마케팅 자산)

홈에 노출되는 "오늘의 발견" 카드 = **블로그 글 + 앱 데이터 결합 콘텐츠**

CMS 별도 구축 없이 어드민에서 마크다운으로 작성:

```typescript
type Insight = {
  slug: string;
  title: string;
  subtitle: string;
  thumbnail: string;
  bodyMarkdown: string;        // 본문
  relatedProductIds: string[]; // 본문 안에 카드로 임베드
  category?: string;
  publishedAt: Date;
};
```

본문 안에 `[[product:id]]` 같은 토큰을 넣으면 앱에서 제품 카드로 렌더링.

런칭 콘텐츠 (3편 추천):
1. "한국 마트 오렌지주스 30개 — 진짜 과즙 함량 전수조사" → 주스 카테고리 유입
2. "스팸 vs 리챔 vs 로스팜 — 돈육 함량으로 본 진짜 가성비" → 햄
3. "냉동만두 라벨 읽는 법 — 고기 함량의 진실" → 만두

---

## 9. 분석 이벤트 (출시 시 필수)

- `app_open`
- `category_view` (category_slug)
- `product_view` (product_id, from_screen)
- `sort_changed` (category_slug, sort_field)
- `compare_added` (product_id)
- `compare_view` (product_ids[])
- `search_performed` (query, result_count)
- `insight_view` (slug)
- `product_request_submitted` (search_query)

KPI:
- D1, D7, D30 리텐션
- 카테고리 진입 → 제품 상세 진입 전환율
- 비교 화면 사용률
- 세션당 본 제품 수

---

## 10. 우선순위 (구현 순서)

### Phase 1 (3-4주, 출시 가능 상태)
- 카테고리 목록 / 랭킹
- 제품 상세
- 비교 (2개만)
- 검색
- 기본 분석 이벤트

### Phase 2 (출시 후 4주)
- 콘텐츠 (인사이트 카드)
- 비교 3개로 확장
- 바코드 스캔
- 사용자 제품 등록 요청

### Phase 3 (D+60-90)
- 추정 원가율 인덱스 (차별화 기능 — 런칭 콘텐츠와 함께)
- 즐겨찾기 / 공유
- 푸시 알림 (신제품 알림, 가격 변동)
- 광고 / 제휴 커머스

---

## 11. 클로드 코드에게 전달할 핵심 지시사항

1. **어드민 DB를 그대로 읽는 구조**로 시작. 본 앱용 별도 DB 만들지 말기
2. **카테고리 1개부터 동작 확인** (햄/소시지) → 그 다음 만두, 주스 순서로 추가
3. **카테고리별 핵심 지표 필드는 Category 메타에서 읽도록** — 하드코딩 절대 금지. 새 카테고리 추가 시 코드 수정 없이 어드민에서만 작업 가능해야 함
4. **비교 화면은 클라이언트에서 조합 X** — 백엔드에 `/compare?ids=a,b,c` 엔드포인트 두고 한 번에 받아오기
5. **이미지는 thumb/medium/full 3종**으로 미리 만들어 두기 (앱 성능)
6. **다이어트 어휘는 코드/UI 어디서도 사용 금지** — 카피 가이드라인 명확히
7. **광고 영역은 처음부터 컴포넌트로 자리 잡아두기** — 비활성화 플래그로 끄고, 나중에 켜면 됨
